// Essentially links the components to the page files we call them in

/* Example:
    export { default as RectButton } from './RectButtons'; // export the RectButton component... imported as "RectButton"
*/


export { default as RectButton } from './RectButton';
